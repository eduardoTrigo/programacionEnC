#include <stdio.h>
#include <string.h>
#define MAX 8

int main(){
    char password[];
    int longitud, max=0, min=0, num=0;
    printf("ingrese una contraseña:\n");
    gets(password);
    longitud = strlen(password);
    for (int i = 0; i < longitud ; i++)
    {
        if (arreglo[i]==)
        {
            /* code */
        }
        
    }
    printf("la contraseña ingresada es:\n");
    puts(password);
    printf("longitud:\n%d", longitud);

    return 0;
}